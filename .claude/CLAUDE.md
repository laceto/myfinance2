# myfinance2 — Agent Router

You are working on a quantitative finance system for Borsa Italiana analysis.
This file routes you to the right context. Load only what you need.

---

## What is your task?

**Coding** — implementing features, new signals, scripts, fixing bugs in source code
→ READ: `coding-rules.md`

**Testing** — writing or expanding test coverage
→ READ: `testing-rules.md`

**Debugging** — investigating failures, tracing data pipeline issues
→ READ: `architecture.md` + `coding-rules.md`

**CI / Workflows** — modifying `.github/workflows/` or scheduled jobs
→ READ: `architecture.md` (CI section) + `git-rules.md`

**Environment / Setup** — installing dependencies, running scripts
→ READ: `environment.md`

**Architecture question** — understanding data flow, algoshort modules, config, schema
→ READ: `architecture.md`

**Committing** — staging and writing a commit
→ READ: `git-rules.md`

---

## Load Your Rules Now

1. Identify your task above
2. Load only the file(s) listed for that task
3. Proceed with that file's context
4. Do not load other files unless explicitly directed

If your task spans two categories (e.g. coding + commit), load both files.
If you are unsure which category applies, ask — do not guess.
